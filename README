This is an APPLICATION for people who want to pretend to be a BOOKSHOP.
It uses an SQLITE database via the SQLALCHEMY package!!


Prerequisites:
fastapi==0.75.0
uvicorn==0.17.6
sqlalchemy==1.4.32
python-dotenv==0.19.2
(validators==0.18.2)


Future updates:
x Add endpoint to view orders and stock
x Before placing an order check that the book exists in the stock table
- Add stock update endpoint (title and author to be verified against stock table, only stock number updated)
- Before placing an order check that the stock is greater than the order quantity
- Combine book and stock base models
- Add some proper error messaging
- Add customer table
- Add stock update endpoint (title and author to be verified against stock table, only stock number updated)
- Add 'order completed' column and remove stock accordingly when updated
- Add dewey decimal system classification with validators
- Move create update delete actions to crud file
- Avoid duplicate book listings
- Add timestamps to orders